test:
  post:
    - echo test

deployment:
  master:
    branch: master
    codedeploy:
      codedeploy-test:
        application_root: /
        deployment_group: codedeploy-test-group
        deployment_config: CodeDeployDefault.OneAtATime
        revision_location:
          revision_type: S3
          s3_location:
            bucket: buzztaiki-codedeploy-test
            key_pattern: codedeploy-test-{SHORT_COMMIT}-{BUILD_NUM}
        region: ap-northeast-1
